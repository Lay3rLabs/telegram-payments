version: "3"

includes:
  config:
    taskfile: ./config.yml
    flatten: true

tasks:
  start-chains:
    cmds:
      - sh "{{.CHAINS_DIR}}/start_neutron.sh"

  stop-chains:
    cmds:
      - docker kill neutron-local || true
      - docker rm neutron-local || true
