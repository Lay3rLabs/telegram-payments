version: "3"

includes:
  config:
    taskfile: ./config.yml
    flatten: true

tasks:
  start-chains:
    cmds:
      - starship start --config "{{.STARSHIP_CHAIN_FILE}}"

  stop-chains:
    cmds:
      - starship stop --config "{{.STARSHIP_CHAIN_FILE}}"
